spring.application.name=ClassExchange

# Perfil ativo padrão (alterado conforme necessário)
spring.profiles.active=dev

# Configuração básica do H2 Database
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.datasource.platform=h2

# Configuração para mostrar SQL no console
# spring.jpa.show-sql=true

# Dialeto do JPA para o H2 Database
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configuração para o comportamento
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.sql.init.mode=always